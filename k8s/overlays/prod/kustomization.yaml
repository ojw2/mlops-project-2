apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

images:
  - name: iris-classifier
    # Replace OWNER with your GitHub username/org. CI updates newTag on each release.
    newName: ghcr.io/OWNER/iris-classifier
    newTag: latest
