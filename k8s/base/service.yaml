apiVersion: v1
kind: Service
metadata:
  name: iris-classifier
  labels:
    app: iris-classifier
spec:
  type: ClusterIP
  selector:
    app: iris-classifier
  ports:
    - name: http
      port: 80
      targetPort: 8000
